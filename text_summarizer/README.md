# Text Summarizer

This folder holds the training notebook. The serving app lives at the project root in `app.py`.

Use:

```bash
cd "/Users/aayannoori/Desktop/Deep Learning /TextSummarizer"
source .venv/bin/activate
jupyter notebook
```

Notes:

- Open `text_summarizer/textsummarizer.ipynb` with the `Python (backend-env)` kernel.
- The training notebook still needs the SAMSum CSV files if you want to retrain the model.
